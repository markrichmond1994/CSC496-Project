docker pull markrichmond1994/csc496-nfs:latest
docker run --privileged --name myNFS markrichmond1994/csc496-nfs:latest /bin/bash

sudo docker service create  –mount type=volume,volume-opt=o=addr=<//insert IP addr here//>, volume-opt=device=:/var/nfs,volume-opt=type=nfs,source=vol, target=/mount –replicas 3 –name nfsTest dockerimage

sudo docker service ps nfsTest
